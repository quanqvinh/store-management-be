export enum IdentifierType {
	EMAIL = 'Email',
	USERNAME = 'Username',
	MOBILE = 'Mobile',
}

export enum TokenSubject {
	ACCESS = 'access',
	REFRESH = 'refresh',
}

export enum Gender {
	MALE = 'male',
	FEMALE = 'female',
	OTHER = 'other',
}
